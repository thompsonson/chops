{
  "identifier": "default",
  "description": "Default capabilities for the chops app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:default",
    "fs:default",
    "notification:default"
  ]
}
